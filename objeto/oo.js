// CÓDIGO NÃO EXECUTÁVEL!!!

// Procedural
processamento(valor1, valor2, valor3)

// OO Orientação a Objetos
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //...
    }
}

objeto.processamento() // Foco passou a ser o objeto

// Principios importantes:

// 1. Abstração = pegar um objeto do mundo real e traduzir para o sistema.

// 2. Encapsulamento = ter os detalhes de implementação escondidos, e mostrar uma interface simples para o usuário.

// 3. Herança (prototype) = recebe atributos e comportamentos de um objeto pai

// 4. Polimorfismo = multiplas formas